[tools]
go = "1.26.2"

[env]
GOPROXY = "https://goproxy.cn,direct"

[tasks.fmt]
description = "Format Go source files"
run = "go fmt ./..."

[tasks.lint]
description = "Run golangci-lint"
run = "golangci-lint run"

[tasks.test]
description = "Run Go tests"
run = "mise exec -- go test ./..."

[tasks.build]
description = "Build rw-node-go"
run = "go run ./cmd/rw-build -o bin/rw-node-go -repo-root ."

[tasks.contract-diff]
description = "Check official remnawave/node contract drift"
run = "go run ./cmd/contract-diff"

[tasks.docker-build]
description = "Build the local Docker image"
run = "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/docker-build.ps1"

[tasks.preflight]
description = "Run release preflight checks"
run = "pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/preflight.ps1"

[tasks.clean]
description = "Remove local build artifacts"
run = "powershell -NoProfile -Command \"Remove-Item -Recurse -Force bin,dist,build,coverage -ErrorAction SilentlyContinue\""
